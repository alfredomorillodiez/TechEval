# remove-local-ai-generation

Elimina la generación de preguntas por IA local (Ollama) y el flag AllowsAiGeneration; el examen se sigue generando por selección aleatoria de preguntas existentes en BD
